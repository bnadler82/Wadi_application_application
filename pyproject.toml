[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "streamlit-app"
version = "0.1.0"
dependencies = [
    "streamlit>=1.30.0",
    "openai>=1.0.0"
]
